{% extends "admin/layout.html.twig" %}

{% block title %}Film{% endblock %}

{% block body %}
    <h1>Film</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ film.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ film.Titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ film.Description }}</td>
            </tr>
            <tr>
                <th>Affiche</th>
                <td>{{ film.Affiche }}</td>
            </tr>
            <tr>
                <th>BandeAnnonce</th>
                <td>{{ film.BandeAnnonce }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_film_controller_crud_index') }}">back to list</a>

    <a href="{{ path('app_film_controller_crud_edit', {'id': film.id}) }}">edit</a>

    {{ include('film_controller_crud/_delete_form.html.twig') }}
{% endblock %}
